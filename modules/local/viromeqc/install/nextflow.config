process {
    withName: VIROMEQC_INSTALL {
        ext.args = '--zenodo'
        publishDir = [
            path: { "${params.outdir}/VirusEnrichment/viromeqc/install" },
            mode: params.publish_dir_mode,
            pattern: '*/',
            enabled: params.save_viromeqc_db
        ]
    }
}
